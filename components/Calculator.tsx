"use client";
import { useMemo,useState } from "react";
export default function Calculator(){const [span,setSpan]=useState(8);const [spacing,setSpacing]=useState(5);const [load,setLoad]=useState(3);const result=useMemo(()=>({area:(span*spacing).toFixed(1),line:(load*spacing).toFixed(1),total:(span*spacing*load).toFixed(1)}),[span,spacing,load]);return <div className="calc"><div><span className="eyebrow">Interactive demonstration</span><h3>Preliminary tributary-load tool</h3><p>A simple example showing how calculation tools can live inside the website. It is not a design verification tool.</p><label>Member span (m)<input type="number" min="1" step="0.1" value={span} onChange={e=>setSpan(+e.target.value)}/></label><label>Tributary spacing (m)<input type="number" min="1" step="0.1" value={spacing} onChange={e=>setSpacing(+e.target.value)}/></label><label>Area load (kN/m²)<input type="number" min="0" step="0.1" value={load} onChange={e=>setLoad(+e.target.value)}/></label></div><div className="results"><div><small>Tributary area</small><strong>{result.area} m²</strong></div><div><small>Equivalent line load</small><strong>{result.line} kN/m</strong></div><div><small>Total load</small><strong>{result.total} kN</strong></div><p>For concept illustration only. Final engineering design requires complete project data and code checks.</p></div></div>}
