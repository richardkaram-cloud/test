"use client";
import { FormEvent } from "react";
import { site } from "../app/content";
export default function ContactForm(){function send(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const subject=encodeURIComponent(`Project inquiry — ${f.get("name")}`);const body=encodeURIComponent(`Name: ${f.get("name")}\nCompany: ${f.get("company")}\nEmail: ${f.get("email")}\nPhone: ${f.get("phone")}\nProject type: ${f.get("type")}\n\nMessage:\n${f.get("message")}`);window.location.href=`mailto:${site.email}?subject=${subject}&body=${body}`;} return <form className="form" onSubmit={send}><div className="formGrid"><label>Name*<input name="name" required/></label><label>Company<input name="company"/></label><label>Email*<input name="email" type="email" required/></label><label>Phone<input name="phone"/></label></div><label>Project type<select name="type"><option>Structural Engineering</option><option>Quantity Surveying</option><option>Structural Review</option><option>Digital Engineering Tool</option><option>Other</option></select></label><label>Project details*<textarea name="message" rows={6} required/></label><button className="button" type="submit">Prepare email inquiry</button><small>The form opens your email application. No information is stored by this website.</small></form>}
